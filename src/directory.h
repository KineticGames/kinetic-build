#ifndef KINETIC_BUILD_TARGET_H
#define KINETIC_BUILD_TARGET_H

// std
#include <stdbool.h>

bool create_dir(const char *path);
bool directory_exists(const char *path);

#endif // KINETIC_BUILD_TARGET_H
